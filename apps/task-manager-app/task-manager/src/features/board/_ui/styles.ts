import { Flex } from "antd";
import styled from "styled-components";
import { backgroundColor } from "@shared/ui";

export const StyledFlex  = styled(Flex)`
    padding: 10px;
    background: ${backgroundColor};
    opacity: 0.7;
`